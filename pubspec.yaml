name: neom_core
description: Core for Open Neom Modules.
version: 1.4.1-dev
homepage: https://github.com/Open-Neom/neom_core
publish_to: 'https://pub.dev/publishers/openneom.dev/packages'


environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^5.0.0-release-candidate-9.3.2

  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0
  cloud_functions: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_auth: ^6.0.0
  firebase_messaging: ^16.0.0
  firebase_crashlytics: ^5.0.0
  googleapis_auth: ^2.0.0

  logger: ^2.6.1
  http: ^1.4.0
  hive_flutter: ^1.1.0

  enum_to_string: ^2.2.1
  path_provider: ^2.1.5

  geolocator: ^14.0.2
  geocoding: ^4.0.0
  google_maps_flutter: ^2.12.3
  google_api_headers: ^4.5.7
  neom_maps_services: ^1.1.0
  neom_google_places: ^1.1.0

  upgrader: ^11.4.0
  package_info_plus: ^8.3.0

  in_app_purchase: ^3.2.3

  rxdart: ^0.28.0
  in_app_purchase_android: ^0.4.0+2
  in_app_purchase_storekit: ^0.4.3
  html: ^0.15.6
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

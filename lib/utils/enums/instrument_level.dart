enum InstrumentLevel {
  novice("novice"),
  intermediate("intermediate"),
  advance("advance"),
  expert("expert"),
  notDetermined("notDetermine");

  final String value;
  const InstrumentLevel(this.value);
}

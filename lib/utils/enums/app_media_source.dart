enum AppMediaSource {
  offline('offline'),
  internal('internal'),
  spotify('spotify'),
  youtube('youtube'),
  google('youtube'),
  other('other');

  final String value;
  const AppMediaSource(this.value);

}

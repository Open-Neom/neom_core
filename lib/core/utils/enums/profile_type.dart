enum ProfileType {
  instrumentist("instrumentist"),
  facilitator("facilitator"),
  host("host"),
  fan("fan"),
  band("band"),
  researcher("researcher");

  final String value;
  const ProfileType(this.value);
}
